<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.muite.zongshe1.mapper.RouteMapper">


    <select id="selectByStartAndDestination" resultType="com.muite.zongshe1.entity.Route">
        select * from route where start=#{start} and destination=#{destination}
    </select>
</mapper>